version: "3.9"

services:
    app:
      build: ./app
      container_name: devops_app
      ports:
        - "5000:5000"
    nginx:
      build: ./nginx
      container_name: devops_nginx
      ports:
        - "80:80"
      depends_on:
        - app
